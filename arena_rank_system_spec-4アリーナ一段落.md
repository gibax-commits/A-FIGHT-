# ARENA ランク制度 仕様メモ

> A-FIGHTのARENAに導入するランク制度(進行・敵バリエーション・UI改善・報酬・戦闘フィールドに関わる土台)についての設計合意事項まとめ。
> **今回の更新**:**敵AIの大規模改修(全4段階)が完了**。交互行動制・敵の武装システム・新アーキタイプ2種・
> 新スキル9種を反映し、未確定項目リスト(8節)を整理し直した。あわせてTYPE-A/B/Cの立ち絵実装も反映。
> 戦闘そのものの詳細(交互行動・敵の武器選択・飛行・新スキル・UI構成)は「ARENA BATTLE 詳細仕様書 v5」を参照。
> 前回の更新では、敵のアクティブスキル(4-4節)を実装済みの内容に書き換え、戦闘フィールド(地形システム)の章(6節)を追加している。
>
> **現在の実装状況(概要)**:ランク制度・練習試合/公式試合/公式大会/世界大会/**Sランク防衛戦**・敵の難易度スケーリング・敵の個体名/ライバル/オーパ・敵のアクティブスキル(オーパ専用4種を含む)・報酬(マテリアル/大会限定アイテム/オーパ報酬/ユニークエフェクト)・戦闘フィールド21マップ、すべて実装済み。**G→S→連覇/防衛という進行が完成した**。
> 戦闘フィールドはサイズ可変(4×3〜7×4)に対応済み。残る大きな未実装項目は**ペーパードール本実装**(型画像・ユニークエフェクトの描画)。敵AIの大規模改修は全4段階が完了済み(8-3節)。

---

## 0. 全体像

- ランクは **G(最低)→F→E→D→C→B→A→S(最高)** の8段階
- Gランクで練習試合を重ねてポイントを貯め、Fランクへ昇格して初めて公式試合・公式大会に出場できるようになる
- Aランクの必要ポイントを貯めきると「世界大会」への挑戦権を得る
- 世界大会優勝で最高位のSランクとなり、以後は防衛戦を行う(7節)
- Sランク中も700ptを貯めれば世界大会に再挑戦でき、優勝すれば連覇となる

---

## 1. ランクポイントの基本ルール

- ポイントは**ランクごとに新しく用意されるメーター**(前のランクからの累計ではなく、都度0からのスタート)
- 昇格に必要なポイントは、ランクが上がるごとに100ずつ増える

| 昇格 | 必要ポイント |
|---|---|
| G → F | 100 |
| F → E | 200 |
| E → D | 300 |
| D → C | 400 |
| C → B | 500 |
| B → A | 600 |
| A → (世界大会 挑戦権) | 700 ※要確認、7節参照 |

- **勝利時の加点**:練習試合・公式試合ともに固定+10ポイント
- **敗北時の減点**:公式試合で敗北すると減点される(F以降のみ。具体的な減点数は未確定 → 7節)
- 練習試合はGランクでしか行えない

---

### 1-2. セーブの扱い(自動セーブ)

ランクや王座の状態が変わる大きな節目では**自動セーブ**が走る(手動セーブし忘れによる取りこぼしを防ぐため)。

| タイミング | 保存内容 |
|---|---|
| ランクの昇格・降格 | 新しいランクとポイント |
| 世界大会 優勝 | Sランク昇格・ポイント0・獲得した報酬 |
| 世界大会 敗退 | ポイント0リセット |
| 防衛戦 敗北 | ポイント50固定・防衛戦の停止フラグ |

自動セーブ時はログに「[SYSTEM] 自動セーブしました。」と表示される。

> **実装上の注意**:報酬アイテムやマテリアルの付与は非同期に反映されるため、
> 自動セーブには**付与直後の値を明示的に渡す**必要がある(`rewardOverrides`引数)。
> これを怠ると「ランクのリセットは保存されるのに、獲得した限定装備は保存されない」という
> 中途半端な状態になる(実際に発生した)。

---

## 2. 降格ルール

- **F以降**、ポイントが0を切ったら**前のランクへ降格**する
- 降格時の着地点は、**降格先ランクの必要ポイントの半分の位置**からスタート(例:E→F降格なら、Fの必要100ポイント中50から再開)
- **例外**:Fランクだけは特例で、ポイントが0を切ってもGランクへは降格しない(Fに留まる)
- Gランクはそもそも降格の概念が無い(最下層)

---

## 3. 試合形式(全て実装済み)

### 3-1. 練習試合(Gランクのみ)
- 現行のARENA通常バトルと同一形式
- 勝利:+10ポイント(固定)

### 3-2. 公式試合(Fランク以降)
- 現行のARENA通常バトルと同一形式(単発の1戦)
- 勝利:+10ポイント(固定)
- 敗北:ポイント減点(数値未確定、現状は仮値-10で実装)

### 3-3. 公式大会(Fランク以降)
- **5回戦のトーナメント形式**(4回戦目=準決勝、5回戦目=決勝)
- 各回戦の間でHP/AP/アクティブスキル(EX_SPECIALSの使用済み状態)は**全回復**する
- **1回でも負けたら即敗退**。敗退した場合、ポイント・報酬は一切得られない(優勝した場合のみ報酬を得る、一発勝負のハイリスク・ハイリターン形式)
- **エントリーコスト**:参加時にランクポイントを50消費する(結果に関わらず消費される)
  - エントリーコストの消費によってポイントが0を切った場合も、2節の降格ルールがそのまま適用される
- **エントリー条件**:保有ポイントが50以下の場合は出場不可(51以上必要)
- **優勝報酬**:5節参照

### 3-4. 世界大会(Aランク限定・公式大会とは別枠)(実装済み)

**実装値**:難易度倍率×1.5(通常のAランク×1.4より高い)、ライバル遭遇率50%(通常25%、4体全員がプールに入る)、優勝報酬はマテリアル+8000とソブリン級の限定装備。
- Aランクの必要ポイント(700)を貯めきることが参加条件(=公式大会とは完全に別の挑戦枠。Aランクの通常の公式大会サイクルとは独立している)
- 追加のエントリーポイント消費は無し(Aランクを1本貯めきること自体が十分に重い参加条件のため)
- 形式は公式大会と同じ5回戦トーナメントを想定(要確認 → 7節)
- **結果に関わらず、Aランクのポイントは0にリセットされる**:
  - 優勝:Sランクに昇格。Aランクのポイントは0に
  - 敗退(トーナメント中のどこかで負け):Aランクのまま。Aランクのポイントも0に
  - どちらの結果でも、次に世界大会へ再挑戦するにはAランクの700ポイントを再度貯め直す必要がある

---

## 4. 敵のランク別バリエーション・難易度スケーリング(実装済み)

今回のランク制度検討の発端になった項目。3層構造+アクティブスキル+個体名で構成する。

### 4-1. 層1:ランク別の絶対難易度倍率(実装済み)

現行の敵ステータス計算(プレイヤーの現在ステータスに対する割合、例:HP×1.2)に、ランク別の倍率をさらに上乗せする。

| ランク | 倍率 |
|---|---|
| G | ×1.0 |
| F | ×1.05 |
| E | ×1.1 |
| D | ×1.15 |
| C | ×1.2 |
| B | ×1.3 |
| A | ×1.4 |
| S(防衛戦) | ×1.5 |

### 4-2. 層2:ランク内のばらつき(実装済み)

層1の倍率に対し、**±15%程度のランダムなブレ**を追加で掛ける。同じランクでも試合ごとに強さの当たり外れが生まれる。

### 4-3. 層3:敵のアーキタイプ(戦い方の個性)(実装済み)

数値の強さだけでなく、プレイヤー側の新機能(SPD回避・ACC/EVA・シールド強化DEFENDなど)と噛み合う"個性"を敵にも持たせる。

| アーキタイプ | 特徴 |
|---|---|
| アタッカー型 | 高ATK。**常に接近して殴る。防御はほぼしない**(HP20%未満で30%) |
| タンク型 | 高DEF/HP。防御時の軽減率がシールド相当に強化。**前に出て粘り、HP50%未満から高頻度(50%)で防御** |
| 回避型 | 高SPD+EVAボーナス。**距離3を維持し、近づかれたら離れる/回り込む。間合いが保てていても50%でレーンをずらす** |
| 照準型 | ACCボーナス。**レーン合わせを最優先(100%)し、射線の確保を狙う** |
| **空戦型** | SPD+15%・ACC+10。補助ユニット装備。**射程圏に入ると上空へ舞い上がり、高度差ぶん射程を伸ばして撃ち下ろす** |
| **強襲型** | ATK+35%・SPD+20%。補助ユニット装備。**空から最短距離で詰めて殴る。障害物も木も無視するため遮蔽が通用しない** |

ランク別の解禁(実装済み):
G/F/Eはアタッカー・タンクのみ → **D/Cで回避型と空戦型** → Bで照準型 → **Aで強襲型**が追加。
強襲型をA以降限定にすることで、上位ランクの脅威として立たせている。

**行動指針(実装済み)**:当初はステータスが違うだけで行動は全型共通だったが、型ごとに間合い・防御頻度・射線の意識を変えるようにした。詳細な判定順とパラメータは「ARENA BATTLE 詳細仕様書」10-1節を参照。
戦闘中は下部のENEMY DATA欄に、その型が何をしてくるかの説明文も表示される。

**敵の武装(実装済み)**:敵も武器を持つようになった(射程・残弾・HEATを管理)。
武装構成はアーキタイプ×ランクで決まり、例えば照準型の高ランクなら背部射撃2門+射撃1になる。
詳細は「ARENA BATTLE 詳細仕様書」6-3節を参照。

**敵の飛行(実装済み)**:空戦型・強襲型は飛行できる。プレイヤーと同じく残ターン制限(4ターン)があり、
尽きると強制着地する。常時飛ばれるとプレイヤーの近接装備が無力化されるため、この制限は必須。

### 4-4. 敵のアクティブスキル(EX_SPECIALS)(実装済み)

ランクが上がるほど、敵もアクティブスキルを使うようになる。

**そのまま流用できたもの**(自分自身に効く効果):
- HEAL / CRITICAL / EVASION / SHIELD
- 敵がEVASION/SHIELDを構えている場合、プレイヤーの攻撃が無効化/半減される(全攻撃手段に適用)

**新規に設計したもの**:
- **WEAKEN / ARMOR_BREAK**:プレイヤーのATK/DEFを下げる効果。プレイヤーのステータスは装備から毎回再計算される値のため、**倍率(atkDebuff/defDebuff)としてplayerに保持し、参照時に掛ける**方式で実装(1=デバフ無し、WEAKEN=0.7、ARMOR_BREAK=0.5)。戦闘中ずっと持続
- **STUN**:**「次のプレイヤーターン開始時のAPを0にする」**。アクティブスキル(0AP消費)はそのターンでも使用可能
- **AP_BOOST**:**「そのラウンドに敵が2行動する」**。接近が必要な状況で移動した後、もう1歩踏み込む形で実装

**ランク別解禁スキル数・プール**(実装値):

| ランク | スキル所持数 | 解禁プール |
|---|---|---|
| G/F | 0 | なし |
| E/D | 1 | HEAL, CRITICAL |
| C | 1 | 上記 + EVASION, SHIELD |
| B | 2 | 上記 + EVASION, SHIELD |
| A | 2 | 既存8種 + 攻撃系3種(貫通射撃/爆裂弾/焼夷弾)+ 行動阻害系4種(移動封鎖/武装凍結/武装破壊/重力異常) |
| S | 3 | 上記 + 捨て身の一撃 + スキル封印(=全17種) |

所持スキルは戦闘開始時にプールから重複なしで抽選される(pickEnemySkills)。各スキルは1戦闘につき1回のみ使用。

**発動条件(優先順位、実装済み)**:
1ターンにつき自己バフ/デバフ系スキルは1つだけ判定(上から見て最初に条件を満たしたものを発動)。AP_BOOSTのみ別枠で判定する。

| スキル | 発動条件 |
|---|---|
| WEAKEN / ARMOR_BREAK | 未使用なら1ターン目(初手)で使用 |
| STUN | 未使用かつ2ターン目以降で使用(初手は使わない) |
| SHIELD | 未使用かつHP50%未満 |
| HEAL | 未使用かつHP40%未満(最大HPの30%回復) |
| EVASION | 未使用かつHP30%未満 |
| CRITICAL | 未使用かつ、このターン攻撃を行う直前 |
| AP_BOOST(別枠判定) | 未使用かつ、移動した後もまだ接近が必要な時 |

スタン中は敵自身もスキルを使えない(行動全体が封じられる)。

**新スキル9種(実装済み)**:攻撃系4種・行動阻害系5種が追加され、EX_SPECIALSは計17種になった。
クラフト確率は既存と同じ扱い(EXモジュール生成はハッシュ由来のため、狙った系統をある程度絞り込める前提)。
スキルは全て射程無視。詳細は「ARENA BATTLE 詳細仕様書」9-0節を参照。

| 系統 | スキル |
|---|---|
| 攻撃系 | 貫通射撃(1.5倍/防御無視)、爆裂弾(1.2倍+1マス吹き飛ばし)、焼夷弾(0.8倍×3ターン)、捨て身の一撃(2.5倍/自傷10%) |
| 行動阻害系 | 移動封鎖、武装凍結、武装破壊、スキル封印、重力異常 |

行動阻害系は数値デバフと違い「何ができなくなるか」で駆け引きを作る。相手の型によって刺さり方が変わり、
例えば移動封鎖は空戦型に、武装破壊は照準型に効きやすい。

**UI表示**:敵の情報は下部のENEMY DATA欄に集約(名前/ステータス/AP/HPバー/アーキタイプの説明/所持武装/所持スキル/現在の状態)。
プレイヤー自身の状態(実効ATK・DEF、被デバフ、発動中スキル、行動阻害、炎上)はヘッダーにバッジ表示する。

> **実装上の注意**:STUNの表示は当初「次ターン行動不能」の予約フラグを出す設計だったが、フラグを立てる処理と消費する処理が同じexecuteCommand呼び出し内で完結するため、Reactが一度もその状態を描画できなかった。`isStunnedNow`という別フラグを追加し、スタンが実際に発生しているターン中ずっと表示する方式に変更している。
>
> **交互行動制でのSTUN(重要)**:1行動だけ封じる方式では、敵が1ターンに複数回動くようになった結果ほぼ無意味になった。
> 現在は**APを0にしてそのターンの残り手番すべてを奪う**方式。プレイヤーが受けた場合は
> 「ターン終了(PASS)」ボタンでターンを送る。詳細は「ARENA BATTLE 詳細仕様書」1節を参照。

### 4-5. 敵の個体名・ライバルキャラクター(実装済み)

> **注意**:`ENEMY_NAME_POOLS`は**全ランク(G〜S)を必ず定義すること**。
> 定義が無いランクはGランクのプールにフォールバックする作りのため、Sランクの定義が無かった時期に
> 「Sランクの戦いにGランクの敵名(ギナ等)が出る」という不具合が発生した。
> Sランクのプール:ラグナロク / アルテミス / ジェネシス / ソロモン / ヴァルハラ / エンドブリンガー

**ランダム名前プール(procedural、G〜A各ランク6体)**:見た目は無く、テキストの名前+アーキタイプ+ランク別倍率の組み合わせのみ。専用アートが不要なため数を絞る必要はない。

| ランク | 名前プール |
|---|---|
| G | ノイン、ジャンヌ、ルキ、スクラ、ギナ、グリーン |
| F | リフ、スパーク、カレント、フリッグ、レイダー、ノマド |
| E | ブレード、サベージ、パワー、クララ、ボルテックス、リッパー |
| D | アイアンクロウ、サイクロン、グラビティ、スティンガー、バリスタ、レイヴン |
| C | ブリッツ、インフェルノ、タイタン、ヴァルキリー、ネメシス、シャドウファング |
| B | ディアボロ、エクリプス、フェンリル、カグヤ、サーベラス、ジャガーノート |
| A | リヴァイアサン、アポカリプス、パラゴン、オメガ、エレクトラ、インヴィクタス |

**ライバルキャラクター(4体、専用の名前+設定+立ち絵込み)**:TYPE-B/Cの立ち絵と同じ扱い(現状はSVGプレースホルダーでロジックのみ先行実装し、専用立ち絵は用意でき次第差し替え)。

| 名前 | ステータステーマ | 配置ランク | 特記 |
|---|---|---|---|
| セリス | ATKボーナス | B | - |
| マキシマム | DEFボーナス | B | - |
| ベステファ | SPDボーナス | A | - |
| ティニー | (ボーナステーマ無し) | A | 専用EXスキル「リミッター解除」持ち。ATK/SPD/EVAを**3ターン固定**で上昇させる(2ターン目以降に自動発動、1戦闘1回) |

**出現プールの構成**:
- G〜C:完全procedural。そのランクの名前プールからランダム選出+アーキタイプの組み合わせのみ(ライバル無し)
- B/A:約75%はそのランクの名前プール+アーキタイプ、残り約25%はライバル(B:セリス・マキシマム、A:ベステファ・ティニー)
- 世界大会:ランダム名前+アーキタイプの敵と、ライバル4体全員を混ぜたプールからランダムに選出(`includeWorldRivals`引数で切り替える。世界大会・Sランク防衛戦の双方で使用中で、遭遇率は50%)

---

## 5. 報酬の全体方針

### 5-1. マテリアル報酬(確定・実装済み)

公式試合よりも公式大会、公式大会よりも世界大会の方が報酬が大きくなる傾斜をつける。公式大会優勝時のマテリアル報酬:

| ランク | マテリアル |
|---|---|
| G / F | 2000G |
| E | 2200G |
| D | 2400G |
| C | 2600G |
| B | 3000G |
| A / S | 4000G |

### 5-2. 大会限定アイテム(実装済み)

通常のO.P.A-GENERATORのクラフトでは出せない高性能アイテムを、公式大会優勝時の限定報酬として用意する。名前は「ランク名+カテゴリー接尾語」の自動生成方式(既存の生成ロジックを流用)。

**ランク名**(G/F/E/D/C/B/A/Sの頭文字に対応):

| ランク | 名称 |
|---|---|
| G | グッピー |
| F | フォックス |
| E | イーグル |
| D | ドレイク |
| C | ケルベロス |
| B | ベヒモス |
| A | アトラス |
| S | ソブリン |

**カテゴリー接尾語**(11カテゴリー全て対応):

| カテゴリー | 接尾語 | カテゴリー | 接尾語 |
|---|---|---|---|
| MELEE | ソード | HEAD | バイザー |
| SHOOT | ランチャー | TOPS | アーマー |
| SHIELD | シールド | BOTTOMS | グリーヴ |
| BACK_MELEE | ブレード | INNER | スーツ |
| BACK_SHOOT | キャノン | SHOES | ブーツ |
| BACK_SUPPORT | ウィング | | |

**生成ロジック**:
- カテゴリーをランダムに1つ選出し、「(そのランクの名称)+(カテゴリー接尾語)」を名前にする(例:Eランクで「イーグルランチャー」)
- アーキタイプは**heavy固定**(通常のランダム抽選ではなく強制)
- 全ステータスに**+30%**の性能ブースト(暫定値)
- ユニークエフェクトは**確定(100%)**で付与(5-3節。優勝賞品としての特別感を出すため、通常クラフトの15〜20%より大幅に高い発生率にする)

### 5-3. ユニークエフェクトシステム(実装済み)

以前fx_testで見た目のみ検証していたユニークエフェクトを、実際のO.P.A-GENERATORのクラフト処理(データ付与)として実装済み。5-2節の大会限定アイテムの前提でもある。

- 通常クラフト時の発生率:**18%**(実装値。当初想定の15〜20%の範囲内)
- 大会限定アイテムの発生率:**確定(100%)**(5-2節)
- 種類は4種:

| 種類 | 実装方式 |
|---|---|
| 炎(揺らめく) | CSS(グラデーション+アニメーション)のみ。新規画像素材不要 |
| スパーク(放電) | CSS(発光ブロブの明滅)のみ。新規画像素材不要 |
| 発光(グロー) | CSS(ハローの明滅)のみ。新規画像素材不要 |
| ウェザリング(傷・汚れ) | 専用テクスチャ画像が必要 |

**ウェザリング用テクスチャ素材(用意済み・検証済み)**:
- 「傷エフェクト」(ひび割れ)と「よごれエフェクト」(すす・汚れ)の2種類。どちらも本物のアルファチャンネル持ちであることを確認済み
- 全アイテム共通で使い回す汎用素材(アイテムごとの個別素材は不要)
- 実装方式:multiplyブレンドでアイテム画像に重ねる(ソード画像への実合成テストで見た目を確認済み)
- ウェザリング発動時、2種類のうち**ランダムでどちらか1枚**を使用する(個体差を出すため)

> **注**:データ(uniqueEffectフィールドの付与・ロール)は今回実装するが、実際の視覚効果としての描画は、型(シルエット)画像を使ったペーパードール本実装が完了してから対応する(archetype/oversizeScaleと同様、データは先行・見た目は後追いの扱い)。

---

## 6. 戦闘フィールド(地形システム)(実装済み)

以前は全戦闘で完全に同じ地形配置だったが、フィールドのバリエーションを導入した。

### 6-1. マップ構成

**7種類のフィールドタイプ × 各3パターン = 全21マップ**。戦闘開始時にランダムで1つ選出される(`ARENA_FIELDS` / `pickArenaField()`)。

| フィールド | 特徴 |
|---|---|
| 平原 | ほぼ平地。射線が通りやすく撃ち合い向き |
| 市街地 | 障害物が多い(A=基本、B=ビル群で高台多め、C=廃墟で障害物多め) |
| 森林 | 木々(判別不能)が主役。窪地も少々 |
| 密林 | 木々が密集。手探りの度合いが強い |
| 渓谷 | 高低差が激しく、高さによる射程補正が大きく効く |
| 湿地帯 | 沼地が多く、射程が縮み命中率も下がる接近戦寄り |
| 渡河地点 | 川が横切る。飛行の有無で戦い方が大きく変わる |

**配置は各マップとも完全固定**(ランダム生成ではない)。これにより「進めない詰み配置」が発生しないことを保証できる。

**グリッドサイズもマップごとに可変**(`cols` × `lanes`。未指定なら従来の5×3)。21マップ中7つが非5×3:

| マップ | サイズ | 性格 |
|---|---|---|
| 市街地-C(狭い路地裏) | 4×3 | 逃げ場が少なく即座に接近戦 |
| 渓谷-C(入り組んだ渓谷) | 4×4 | 狭いが高低差が激しい |
| 森林-C(奥行きのある森) | 5×5 | レーンが多く、回り込みの余地が大きい |
| 湿地帯-C(広がる湿原) | 5×5 | 沼地だらけで移動が不利 |
| 密林-C(広大な密林) | 6×4 | 木々の判別をしながら広い範囲を進む |
| 平原-C(広大な平原) | 7×3 | 横に長く、撃ち合いの距離が長い |
| 渡河地点-C(大河) | 7×4 | 最大サイズ。川が縦断し飛行の有無が大きい |

開始位置は**対角線上の最遠**(プレイヤー=1-最手前、敵=最右-最奥)。広いフィールドほど初期距離が開き、接近までの駆け引きが生まれる。

> **検証済み**:全21マップについて、地形キーがサイズ内に収まっていること・開始位置が塞がっていないこと・**斜め移動込みで**敵に到達可能なこと(すべての木が通行不可という最悪ケースを含む)・十分な可動マス数があること・川を渡るAPコストが1ターンのAP上限(`MAX_AP`。検証当時は3、現在は5)を超えないことをスクリプトで検証している。市街地Cと密林3マップが当初この検証に引っかかったため、配置を修正した。

### 6-2. 地形オブジェクト一覧

| 地形 | 高さ | 効果 |
|---|---|---|
| 平地(FLAT) | 0 | なし |
| 高台(HIGH) | +1 | 射程が伸びる |
| 窪地(LOW) | -1 | 射程が縮む |
| 障害物(WALL) | +1 | 射線を遮る。侵入不可(飛行時のみ通過可) |
| **沼地(SWAMP)** | -1 | 窪地の効果+そこにいる側の**命中率-12%**(飛行中は影響を受けない) |
| **川(RIVER)** | 0 | FLY以外の移動で、移動元・移動先のどちらかが川なら**AP消費2**。AP不足時は移動できない |
| **低木(TREE_LOW)** | +1 | 障害物相当(射線を遮る/侵入不可) |
| **高木(TREE_HIGH)** | +1 | 高台相当だが、こちらも射線を遮る/侵入不可 |

敵はAP制ではないため、川がらみの移動は**50%の確率で「足を取られて移動しきれない」**という形で表現している。

**斜め移動**:横+縦を1手で移動できる(AP1)。プレイヤー・敵の双方が可能。レーン数が増えたフィールドで移動が冗長にならないようにするための措置。
射程の「レーン距離1以内」という制約は据え置き(緩和すると照準型の射線づくり・回避型のレーン回避という役割が薄れるため)。

### 6-3. 木々の判別システム

低木と高木は**隣接するまでどちらか判別できない**。これは「地形は常に既知」という従来の前提を初めて崩す機能。

- **判別前は安全側に倒して「障害物」として扱う**(射線を遮り、侵入もできない)
- **判別タイミングは隣接時**(距離1以内。斜めも含む)。フィールドが広くなったら「距離2以内」への変更を検討
- **判別状態はプレイヤー・敵それぞれ別に管理**し、双方とも手探りになる(敵の動きから木の正体が透けて見えるのを防ぐため)
- 開始位置に隣接する木は最初から判別済み扱い
- **未判別の木に踏み込もうとすると正体が判明するが、そのターンは判別のみで移動は成立しない**(手探りのコストを払う設計)。敵も同じ挙動のため、プレイヤーだけが手数で得をすることがない
- 未判別の木はUI上「?」表示になり、判別後に低木/高木のSVGへ切り替わる

> **実装上の注意**:`battleState`は`JSON.parse(JSON.stringify(...))`でディープコピーされるため、判別状態は**Setではなく配列**で保持している(Setだと空オブジェクトに変換されてしまう)。`hasRevealed`/`addRevealed`ヘルパー経由で読み書きすること。

### 6-4. ビジュアル

- 地形は`TERRAIN_VISUALS`にSVGで定義。地形タイプごとに`baseClass`(下地)と`render`(SVG)を持つ構造で、**将来的に画像素材が用意できたら`render`を`<img>`に差し替えるだけで移行できる**(`CHARA_ART` / `WEAPON_VISUALS`と同じ考え方)
- 空(FLY位置)は`SkyBackdrop`コンポーネントで上部が空・下部が地上という縦構造を描画。飛行中のファイターには足元から地面へ伸びる高度線と接地点の光を表示する
- 奥のレーンほど暗くする奥行き表現あり

---

## 7. Sランク・防衛戦システム(実装済み)

### 7-1. Sランクの位置づけ

**Sランクは「肩書き」であり、ポイント上はAランクのメーターを使い続ける**。世界大会優勝でSランク(チャンピオン)となるが、その直後にAランクポイントは0にリセットされ、再び700を貯めて世界大会に挑み、優勝すれば連覇……という繰り返しになる。

### 7-2. 防衛戦の発生

- Sランク保持中、**ARENA入口に入る時に15%の確率で防衛戦が発生**する
- 発生すると挑戦者がすぐに現れ、バトルが始まる
- 相手は**世界大会の敵枠(ライバル4体+ランダム名前の敵)からランダム選出**。ただし**20%の確率で「オーパ」**(7-4節)になる
- 発生確率15%の根拠:700ポイントは長い道のりのため、高すぎるとすぐ次の世界大会に到達してしまい、低すぎると防衛成功時のポイント報酬の意味が薄れる

### 7-3. 挑戦の受諾・拒否と勝敗

| 選択・結果 | 効果 |
|---|---|
| **拒否する** | ランクポイント**-10**(公式試合1敗と同等)。チャンピオンではあるが挑戦から逃げた扱い |
| **勝利** | ランクポイント**+100**(公式大会優勝と同等) |
| **敗北** | ランクポイントが**50に固定**される(現在値がいくつでも50になる。公式大会に参加できないギリギリのライン)。さらに**次に世界大会で優勝するまで防衛戦が発生しなくなる** |

敗北時の「防衛戦が起きなくなる」仕様は、単なる数値減少ではなく**王者としての立場を失う**という物語的な意味を持たせるためのもの。ポイントを貯め直して世界大会で優勝し直すことが、王者の座に戻る道になる。

### 7-4. オーパ(A-FIGHT運営からの刺客)

**実装値**:撃破報酬はマテリアル+12000に加え、以下の比率でランダムに1つ。
| 報酬 | 確率 | 内容 |
|---|---|---|
| オーパコア(EXチップ) | 25% | キャパシティ60 |
| オーパモジュール | 35% | コスト20、専用スキル4種のいずれかを付与 |
| オーパ装備 | 40% | 全11カテゴリー対応、ステータス+60%、ユニークエフェクト確定 |


- 稀に防衛戦を仕掛けてくる特別な敵。ライバル達よりも強い設定
- **難易度倍率1.7**(`OOPA_STAT_MULT`)。**ランク倍率への上乗せではなく絶対値**として扱う。
  当初は+40%の上乗せ方式だったが、Sランクの1.5と二重に掛かって実質2.1倍となり、
  2ターン目に即死する状態になったため絶対値へ変更した(v1.23.1)
- **専用アクティブスキル4種(7-5節)を所持し、実際に使ってくる**
- **撃破報酬(特殊報酬)**:以下からランダム
  - オーパ専用の限定装備(全部位カテゴリーに対応)
  - オーパ製EXチップ(通常クラフト不可)
  - オーパ製EXモジュール(通常クラフト不可。7-5節の4スキルを付与)

### 7-5. オーパ専用スキル4種(新規)

**プレイヤーはオーパを撃破しない限りこの4つを入手・使用できない**(オーパ製EXモジュール経由でのみ入手)。いずれも破格の性能を持つが、その分の後隙(デメリット)も大きいピーキーな設計。

| スキル | プレイヤー版 | 敵(オーパ)版 |
|---|---|---|
| **一斉発射** | 装備中の全射撃武装の**合計ATK**がそのまま与ダメージ(防御・シールド・完全回避で軽減/回避は可能)。使用直後、**全射撃武装の残弾が強制的に0**になりリロード必須 | **ATK×2.5**のダメージ。使用後**2ターン攻撃不可**(再装填している状態) |
| **渾身の一撃** | **位置に関係なく背後を取る**(JUMPと同じ挙動)+**ATK×2の防御貫通**ダメージ(完全回避でのみ回避可能、防御・シールドは無効)。使用直後**2ターンスタン**(無理にジャンプした反動) | 同左(背後を取る挙動・ダメージ・2ターンスタンとも同じ) |
| **オートリペア** | 3ターンのパッシブ効果として、**被ダメージの半分を回復**(200ダメージなら100回復)。効果中は出力を回しているため**FLY状態になれない** | 3ターン、同じ回復効果。敵に飛行の概念が無いため、代わりに**効果中は移動できない** |
| **APハイブースト** | **AP+4**(APブーストの上位互換。デメリット無し) | **3行動**(通常のAP_BOOSTが2行動なのに対して) |

### 7-6. オーパ製EXチップ・モジュールの数値

現在の通常クラフト値:EXチップのキャパシティ16〜35(平均25程度)、EXモジュールのコスト3〜12(平均7程度)。つまり通常は1チップに3〜4個のモジュールを組める。

| アイテム | 数値 | 狙い |
|---|---|---|
| オーパ製EXモジュール | **コスト20** | 通常チップ(最大35)にも1個だけなら入るが、他モジュールとの併用はほぼ不可 |
| オーパ製EXチップ | **キャパシティ60** | オーパモジュールを3個、または2個+通常モジュール数個という構成が可能 |

この関係により「オーパモジュールを手に入れても専用チップが無いと1個しか積めない」「専用チップを手に入れて初めて複数運用できる」という段階が生まれ、チップとモジュールを別々に集める動機になる。

> **数値は要調整**:実際に入手して運用した時の手応えを見てから見直す前提の暫定値。

---

## 8. まだ未確定・未実装の項目

### 8-1. 未実装(次の作業候補)

- **ペーパードール本実装**:型(シルエット)画像+動的着色+オーバーサイズ+ユニークエフェクトの重ね描画。
  現状アイテムは色スウォッチ表示のみで、装備しても見た目に反映されない(サンプルソードのみ例外的に手に描画される)
- **ユニークエフェクトの視覚的な描画**:データ(uniqueEffectフィールド)の付与は実装済みだが、
  実際の見た目への反映はペーパードール本実装後
- **ライバル4体・オーパの専用立ち絵**:ライバルは現状SVGプレースホルダー。
  オーパは暫定的に差し替え前のTYPE-A立ち絵を流用中(`CHARA_ART.OOPA`)
- **大会限定アイテム・オーパ報酬の専用ビジュアル**:通常アイテムと同じ色スウォッチ表示のまま
- **木々の判別範囲の見直し**:現在は「隣接時(距離1)」。フィールドが最大7×4まで広がったので、
  広いマップでは「距離2以内」に緩和するかどうかを実プレイの手応えを見て判断する

### 8-2. 数値の要調整項目(実装済みだがバランス未検証)

- 各ランクの必要ポイント数値(G=100〜A=700の積み上げ)
- 公式試合で敗北した際の減点数(現状-10)
- ライバル4体のステータスボーナス(現状+25%)
- 大会限定アイテムの性能ブースト(現状+30%)
- 沼地の命中率ペナルティ(現状-12%)、川の移動コスト(現状AP2)
- 敵アクティブスキルの解禁ランク・所持数(実装値は4-4節の表の通りだが、実プレイでの手応えは要観察)
- B/Aランクのライバル出現率(現状25%)
- 防衛戦の発生率(15%)・オーパ出現率(20%)・オーパの難易度倍率(1.7)・オーパ製EXチップのCAP(60)とモジュールのコスト(20)
- **斜め移動のコスト**(現状AP1)。理想はAP2。AP上限が5になったので再検討の余地がある
- **被ダメージ上限**(通常70% / 一斉発射75% / 渾身の一撃60%)。理不尽な即死を防ぐ安全弁として導入したもの
- アーキタイプ別行動指針のパラメータ(preferredDist / 防御閾値・確率 / laneAlignChance)
- **MAX_AP = 5**。交互行動制での手数感・1ターンの長さは実プレイでの調整対象
- **敵の武器テーブルの数値**(atkMult / 残弾 / heatRate)。全30種の性能バランスは未検証
- **空戦型・強襲型の解禁ランク**(D以降 / A以降)と**敵の飛行ターン(4)**
- **新スキル9種の倍率と持続ターン**(貫通1.5 / 爆裂1.2 / 焼夷0.8×3 / 捨て身2.5・自傷10%、阻害系は全て1ターン)

### 8-3. 解決済み(記録として)

- ~~フィールドサイズ可変~~ → 実装済み(6-1節)。あわせて斜め移動も追加
- ~~敵AIの意思決定が全アーキタイプで共通~~ → 型ごとの行動指針を実装(4-3節)
- ~~敵AIの大規模改修(敵もAP制・武器持ちに)~~ → **全4段階が完了**
  1. AP5化+交互行動制
  2. 敵の武装システム(専用テーブル30種・残弾/HEAT・飛行ターン制限)
  3. 新アーキタイプ2種(空戦型・強襲型)と敵の飛行
  4. 新スキル9種(攻撃系4・行動阻害系5)
- ~~TYPE-B/Cの立ち絵が未実装~~ → 正規絵を実装済み(TYPE-Aも新しい絵に差し替え)

- ~~WEAKEN/ARMOR_BREAKをプレイヤーに適用するための「戦闘中限定デバフ」の保持方法~~ → 倍率としてplayerに保持する方式で実装済み(4-4節)
- ~~敵アクティブスキルの解禁ランク・所持数プール~~ → 実装済み(4-4節)
- ~~B/Aランク・世界大会の出現プールの正確な枠数~~ → 確率25%として実装済み
