# A-FIGHT 機能ロードマップ

> 実装済み機能の整理と、今後の新機能・新モードの構想をまとめたもの。
> 素材(画像/BGM/SE)の必要一覧は別ドキュメント「A-FIGHT 素材リスト」を参照。
> 実装順は固定せず、その時々で決める方針。ここでは**依存関係と優先度**を整理する。

---

## 0. 現在地

戦闘システムとARENA(ランク制度)は一通り完成している。
一方で、**戦闘に入る前後の体験(導入・育成・世界観)がほぼ空白**という状態。

| 領域 | 状態 |
|---|---|
| 戦闘システム | ほぼ完成(大規模改修 全4段階が完了) |
| ARENA / ランク制度 | 完成(G→S、世界大会、防衛戦、オーパまで) |
| クラフト・装備 | 実装済み(ペーパードールの見た目反映のみ未実装) |
| セーブスロット | 完成(TYPE別3スロット / v1.32.0) |
| シンクロ率の土台 | 段階1完了(現在値/上限の分離・セーブ対応 / v1.34.0) |
| **導入・世界観** | **ほぼ未着手**(タイトル画面は機能するが簡易的) |
| **育成・成長** | **未着手**(装備更新以外に伸びる要素が無い) |
| **ストーリー** | **未着手** |

---

## 1. 土台となる3機能

以下の3つは**他の多くの機能がこれに乗る**ため、先に固めておく必要がある。
逆に言えば、ここが決まらないと上物の設計が確定しない。

### 1-A. セーブスロット(TYPE別) — ✅ 完了

TYPE別にセーブを分け、任意のタイミングでロード・タイトル復帰ができるようにした。
これにより**2章(導入・世界観)の全項目のブロックが外れている。**

- ✅ TYPE別(A/B/C)にスロットを分ける — キーは `afight_save_v2_{TYPE}`、旧キーからの移行処理あり
- ✅ ロードボタンの追加 — タイトル画面のスロットが CONTINUE / NEW GAME を自動判定
- ✅ タイトルに戻るボタンの追加 — ヘッダーの TITLE ボタン。未保存消失のため2度押し確認付き

### 1-B. シンクロ率のブラッシュアップ — 段階1完了 / 段階2は4-Bと同時

**方針**:現在の150%(= x1.50)を**出発点**とし、育成で上へ伸ばす。
現行ARENAの敵倍率は150%前提で調整されているため、この値を下限として守れば
既存のバランス調整に一切手を入れずに済む。倍率式 `getSyncMultiplier` は変更しない。

シンクロ率は2つの値に分けて持つ:

```
syncMax   … 上限。起動シークエンス(2-D)で初期値が決まり、育成(5-A)で引き上げる
syncRatio … 現在値。戦闘で消耗し、メンテナンス(4-B)で syncMax まで回復する
```

#### 段階1(完了 / v1.34.0)

state化・セーブ対応・上限の概念の導入まで。**ゲームの挙動は v1.33.0 と同一。**

- ✅ `syncRatio` / `syncMax` を setter 付き state に分離。セーブデータに追加
- ✅ 旧セーブ互換(`syncMax` 未定義なら `SYNC_BASE` に落とす / 現在値は上限で頭打ち)
- ✅ `SYNC_BASE = 150`(最低保証・現行バランスの基準点)、`SYNC_CAP = 250`(育成の天井 / x2.10)
- ✅ 起動シークエンス用の初期値決定関数 `resolveBootSyncMax(result)` を先行して用意
  - `GREAT: 170 (x1.62) / GOOD: 160 (x1.56) / PLAIN: 150 (x1.50)`
  - **不正解でも150%を保証**するため、初回の選択が取り返しのつかない格差にならない
- ✅ ヘッダーとROOMに上限を表示
- 付随して `saveGame` を位置引数8個からスナップショットオブジェクト受け取りに変更

#### 段階2(4-Bと同時に実施)

消耗と回復のルールを有効化する。**回復手段(4-B)が無い状態で消耗だけ入れると
減り続けて詰むため、単独では進められない。**

- 戦闘での消耗量(試合種別ごとの重み)
- メンテナンスでの回復量
- 育成による `syncMax` の引き上げ段階(5-Aと接続)

> **設計上の注意**:「戦闘で減る/メンテで戻す」は、放っておくと
> 定期的にメンテ画面を開くだけの作業になりがち。4-Bが狙う
> 「正しく接することを探る駆け引き」が成立する頻度と重みに調整する必要がある。
> 頻度が高いと作業、低いとシステムが空気になる。

**これが前提になるもの**:メンテナンスゲージ、メンテナンスモード、育成(最大値引き上げ)、起動シークエンス

### 1-C. ペーパードール本実装

**現状の問題**:装備しても見た目に反映されない(サンプルソードのみ例外的に手に描画)。

- 型(シルエット)画像+動的着色+オーバーサイズ描画
- ユニークエフェクトの視覚的な描画
- 装備カテゴリーごとの重ね順・アンカー定義

**進捗**:アンカー実測(TYPE-A/B/C/A0)と設計は完了済み(v1.33.0)。
`CHARA_ANCHORS` / `ANCHOR_PAIRS` は確定値が入っており、
残るのは **描画処理への接続** と **型(シルエット)画像の用意**。
`DOLL_LAYERS` はスキーマをコメントとして記録した状態で、実データはまだ無い。

**これが前提になるもの**:髪型変更(見た目に反映されないと意味が無い)

---

## 2. 導入・世界観(一連の流れ)

**1-A(セーブスロット)完了により、全項目のブロックが外れている。**
以下は「新しいパートナーを迎える」までの一続きの体験として設計する。

```
タイトル画面
  ↓
オープニング(テキストで世界観を語る)
  ↓
TYPE選択画面(カタログを眺めて購入機体を決める)
  ↓
起動シークエンス(自宅に届いた機体を起動する / シンクロ率初期値が決まる)
  ↓
名付け
  ↓
ROOM(以降が既存のゲーム本編)
```

### 2-A. タイトル画面

セーブスロットUIとしては機能している(1-Aで実装)。
残っているのは**世界観を印象づける入口としての見せ方**で、
ロゴ・背景演出・BGMなどの表現面。

> オープニング(2-B)で世界観のトーンが決まると入口の見せ方も変わるため、
> 2-Bと合わせて着手するほうが手戻りが少ない。

### 2-B. オープニング(テキスト)

A-FIGHTの世界観を語る導入。テキスト送り形式。

### 2-C. TYPE選択画面

**「主人公がカタログを眺めて、購入する女性型アンドロイドを決める」**という描写。

- 1画面につき1体、**サイドスワイプでA→B→Cを切り替える**カタログ風UI
- 各TYPEの立ち絵を大きく表示(実装済みの立ち絵を流用可)
- カタログとしての説明文(スペック・性格傾向)

### 2-D. 起動シークエンス

購入した機体が自宅に届き、初めて起動する場面。**シンクロ率の初期値がここで決まる。**

> **ロジック面のブロックは外れている**(1-B段階1で `resolveBootSyncMax` を用意済み)。
> 実際のブロッカーは**素材**:目閉じの全身立ち絵、バストアップ立ち絵+表情差分。
> これらは4-B メンテナンスモードと共用するため、揃えば2つのモードが同時に動き出す。

**直感的操作による三択**:

| 操作 | 内容 |
|---|---|
| タップ | 触れる |
| スワイプ | 撫でる |
| ホールド(長押し) | 手を添える |

**TYPEごとに「正解」が異なる**(性格設定と対応):

| TYPE | 性格 | 正解の操作 |
|---|---|---|
| TYPE-A | 冷静・生真面目 | 頭を**ホールド**(手を添える) |
| TYPE-B | 純真・従順 | 頭を**スワイプ**(撫でる) |
| TYPE-C | 勝気・ツンデレ | 頭を**タップ**(触る) |

- 正解/不正解で**シンクロ率上昇が大・中・小**に分岐
- それぞれに**TYPE別の反応テキストと表情**を用意
- 会話表示は**画面下部のパネルを切り替える方式**(ARENAの ENEMY DATA と同じ仕組みを流用)
- 立ち絵は**バストアップに切り替えて表情を見せる**(ADVゲーム方式)
- 起動前の演出として**目閉じの全身立ち絵**も使用する
- **この仕組みはメンテナンスモードにも応用する**(素材・実装ともに共用できる)

### 2-E. 名付け

パートナーとなるアンドロイドに名前を付ける。以降ゲーム内で使用される。

---

## 3. ストーリーとチュートリアル

### 3-A. ストーリーモード(新モード)

本編のストーリー。**会話ウインドウ**(戦闘中の全体ログ欄を転用する想定)でテキストを表示する。

### 3-B. チュートリアル

**ストーリーモードと絡めた形**で実装する。単独のチュートリアル画面ではなく、
物語の流れの中で操作を覚えていく設計。

> 現在はテストプレイ前提のため説明が一切無い状態。新規プレイヤーには
> AP・射程・地形・アーキタイプなど覚えることが多いため、段階的な導入が要る。

---

## 4. シンクロ率とメンテナンス

**1-B(シンクロ率のブラッシュアップ)が前提。**

### 4-A. メンテナンスゲージ

シンクロ率の増減に絡むゲージ。戦闘で消耗し、メンテナンスで回復する想定。

### 4-B. メンテナンスモード(新モード)

シンクロ率を回復・維持するためのモード。
**起動シークエンスと同じ直感的操作(タップ/スワイプ/ホールド)を応用する。**

- TYPEごとの正解操作、反応テキストの仕組みを共用できる
- 素材(立ち絵の表情差分、目閉じ全身など)も共用できる
- **メンテナンスする部位/範囲の選択**という要素を持つ
  - 選択を誤ると**羞恥**の反応となり、シンクロ率が若干減少する
  - さらに踏み込んだ失敗は**苦痛**の反応となり、減少幅が大きくなる
- 単なる回復作業ではなく、**正しく接することを探る**駆け引きにする

---

## 5. 育成・カスタマイズ

### 5-A. 育成要素

- **シンクロ率の最大値引き上げ**
- **装備強化**

現状は「より良い装備を引く」以外に強くなる手段が無いため、
継続プレイの動機付けとして機能させたい。

### 5-B. 髪型変更

- CLOSETに**新カテゴリー**として追加
- 入手は**マテリアルを消費して解放**する方式
- 各種装備と同様に**ペーパードールで見た目に反映**(1-Cが前提)

**前髪+サイド / 後ろ髪を別々に選べる方式**にする:

```
[前髪+サイド]   ← 体より手前に描画
[髪無しベース]  ← 顔・耳・首・肩まで描いたもの
[後ろ髪]        ← 体より奥に描画
```

- 別枠で解放できるため、**組み合わせでバリエーションが掛け算で増える**
  (後ろ髪ツインテール × 前髪パターンA/B/C など)
- 髪型1種の追加コストが小さく済む(素材リスト 2-3節)
- **戦闘中の反映は当面見送り**(最小90pxまで縮小され差が見えにくいため)

---

## 6. 補助機能

### 6-A. ゲーム内tips

各種システムの説明。テストプレイ中は不要だったが、
システムが複雑化したため必要性が高まっている。

---

## 7. 構想のみ(優先度低)

### 7-A. 対戦モード

**現時点では優先度を低く見ている。** 理由:

- ストーリー/メンテナンス/育成と、既に大きなモードが3つ控えている
- 対戦は「相手をどう用意するか」が本質的に難しい
- 現在のARENAが実質的に対戦の役割を果たしており、防衛戦・オーパで上位帯の張り合いも成立している

もし実装するなら、**育成が実装された後に「他プレイヤーのセーブデータを模した仮想対戦相手」**という形が現実的。

---

## 8. 依存関係まとめ

```
[1-A セーブスロット] ✅完了 ─┬─→ 2-A タイトル画面      ← 着手可
                              ├─→ 2-B オープニング      ← 着手可
                              ├─→ 2-C TYPE選択画面      ← 着手可
                              ├─→ 2-D 起動シークエンス  ← 1-B待ち
                              └─→ 2-E 名付け            ← 着手可

[1-B シンクロ率]
  段階1 ✅完了 ───────→ 2-D 起動シークエンス(初期値決定)  ← 素材待ち
  段階2 (4-Bと同時) ─┬─→ 4-A メンテナンスゲージ
                      ├─→ 4-B メンテナンスモード          ← 素材待ち
                      └─→ 5-A 育成(最大値引き上げ)

[1-C ペーパードール] ──┬─→ 5-B 髪型変更
                        └─→ (ユニークエフェクトの視覚化)

[3-A ストーリーモード] ─→ 3-B チュートリアル

[2-D 起動シークエンス] ←→ [4-B メンテナンスモード]  ※直感的操作の仕組みを共用
```

**素材不要ですぐ進められるもの**:2-A(見た目のみ)、2-B(テキスト)、
2-C(実装済みの立ち絵を流用)、2-E(UIのみ)、6-A(tips)

**素材待ち**:2-D / 4-B(目閉じ全身・バストアップ+表情差分を共用)、
1-C(型シルエット画像。ただしTYPE-Aの髪だけなら描画接続の検証は可能)

---

## 9. 実装済み機能(参考)

新規実装の設計時に「既にあるもの」を把握するための一覧。

| 機能 | 概要 |
|---|---|
| ROOM | 拠点画面。機体ステータス表示 |
| CLOSET | 装備の着脱(両手・両背部・頭・上下・インナー・シューズ・EXコア) |
| GENERATOR | 単語ハッシュによる装備クラフト |
| ARENA | 練習試合/公式試合/公式大会/世界大会/防衛戦 |
| ランク制度 | G→F→E→D→C→B→A→S、昇格・降格・世界大会・連覇 |
| 戦闘システム | AP制交互行動、可変フィールド、地形、飛行、17種のスキル |
| 敵AI | 6アーキタイプ、専用武器30種、残弾/HEAT管理、飛行 |
| オーパ | 防衛戦の特殊敵。専用スキル4種と専用報酬 |
| セーブ | localStorage、TYPE別3スロット、重要な節目で自動セーブ |
| タイトル画面 | スロット一覧、CONTINUE/NEW GAME判定、データ削除、TITLE復帰 |
| 立ち絵 | TYPE-A/B/C(正規絵)、オーパ(暫定) |
| ペーパードール | アンカー実測データのみ(描画未接続) |
| シンクロ率 | 現在値/上限の2値。上限250%まで。消耗・回復は未実装 |
| BGM/SE | Web Audio APIによる合成音(攻撃・被弾・回避・勝利・敗北等) |
