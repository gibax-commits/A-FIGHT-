# ARENA BATTLE 詳細仕様書 (v5)

> 装備・モジュール種類追加の検討のために作成した仕様書。実装済みの内容を都度反映して更新している。
> **v5での更新**:**大規模改修(全4段階)が完了**。AP5化と交互行動制、敵の武装システム、
> 新アーキタイプ(空戦型・強襲型)と敵の飛行、新スキル9種(攻撃系4/行動阻害系5)を反映した。
> あわせて立ち絵アセットの実装状況(14節)も追記。
> ランク制度・フィールド一覧・報酬など、戦闘そのもの以外の全体設計は「ARENAランク制度 仕様メモ」を参照。

---

## 1. 全体構造

- **横位置 × レーン のグリッド**+**高度(Z軸)**を持つターン制バトル
- **グリッドサイズはマップごとに可変**(4×3〜7×4。未指定なら従来の5×3)
  - 横位置:1〜cols(数値が大きいほど画面右)
  - レーン:1が最奥、lanesが最手前(3レーンならTOP/CENTER/BOTTOM、それ以外はL2..等の表記)
- **開始位置は対角線上の最遠**:プレイヤー=(1, 最手前レーン)、敵=(最右, 最奥レーン)
- 高度:-1(窪地・沼地)/0(平地・川)/1(高台・障害物・木)/2(飛行中)
- **プレイヤー・敵ともにAP制(MAX_AP = 5)の「純粋な交互行動」**
  - プレイヤーが1行動するたびに、敵も1手番を消費して行動する
  - 消費APは行動ごとに異なる(移動1/攻撃2/スキル0)ため、**重い行動を選ぶほど相手に
    手番を多く渡すことになり、その「ズレ」自体が駆け引きになる**
  - スキルは0AP消費なので手番を渡さない(準備行動として機能する)
  - 片方だけAPが尽きた場合は、残っている側が連続で行動する
  - 両者ともAPを使い切った時点でターン終了・APリセット
  - 実装は `runEnemyAction()`(敵の1手番)と `endTurn()`(ターン終了処理)に分離してある

> **STUNとの関係(重要)**:プレイヤーが行動できないターンは`executeCommand`が呼ばれないため、
> ターン処理が進まずデッドロックする。「プレイヤーが行動不能な間はターンを自動で進める」
> ループにしてあり、さらに**ターン終了(PASS)コマンド**でプレイヤー自身がターンを送れる。
> スタンは**APを0にしてそのターンの残り手番すべてを奪う**方式(1行動だけ封じる方式では
> 交互行動制で効果がほぼ無くなるため)。

## 2. 戦闘開始時の初期化(`startBattle`)

| 項目 | 内容 |
|---|---|
| プレイヤー戦闘力(powerLevel) | `HP + ATK×5 + DEF×4 + SPD×4` |
| 敵タイプ | A/B/Cからランダム抽選(ステータス傾向のみ) |
| 敵HP/ATK/DEF/SPD | プレイヤーの `×1.2` / `×0.9` / `×0.8` / `×0.8` |
| 撃破報酬 | `100〜300G` のランダム |
| プレイヤー初期位置 / 敵初期位置 | (1, 最手前レーン) / (最右, 最奥レーン) ※対角線上の最遠 |
| 初期AP | プレイヤー・敵ともに `MAX_AP`(=5) |
| 敵の武装 | `generateEnemyWeapons(archetype, rank)` でアーキタイプ×ランクに応じて生成(6-3節) |
| 敵の飛行 | `maxFlightTurns = 4`(空戦型・強襲型のみ実際に飛ぶ) |

### 装備由来の戦闘用データ生成
- **`R_HAND`/`L_HAND`/`R_BACK`/`L_BACK`** の装備アイテムを見て`activeWeapons`を構築(4スロットに拡張済み。以前は`BACK`1スロットのみだった):
  - `SHOOT`/`BACK_SHOOT` → 射撃武器(`currentAmmo = maxAmmo`)
  - `MELEE`/`BACK_MELEE` → 近接武器(`currentHeat = 0, maxHeat = 100, heatRate`)
  - `SHIELD`等はここには入らない(後述の通り別経路でDEFENDに影響する)
- **`C_BACK`** の装備が`BACK_SUPPORT`タイプなら`hasFly = true`。同じく`C_BACK`の装備から`maxFlightTurns`(無ければデフォルト3)を取得
- **`R_HAND`/`L_HAND`** に`SHIELD`タイプが装備されていれば`hasShieldEquipped = true`、その実際のアイテム名を`shieldName`として保持

---

## 3. フィールド・地形

**戦闘開始時に全21マップ(7種類×3パターン)からランダムで1つ選出される**(`ARENA_FIELDS` / `pickArenaField()` / `ARENA_CURRENT_FIELD`)。各マップの配置自体は完全固定。
**マップごとに横幅(`cols`)とレーン数(`lanes`)が異なる**(4×3〜7×4。未指定なら5×3)。
サイズは`getFieldCols()` / `getFieldLanes()`、開始位置は`getStartPositions()`で取得する。
詳細な一覧・検証内容は「ARENAランク制度 仕様メモ」6節を参照。

地形オブジェクトと効果:

| 地形 | 高度値 | 効果 |
|---|---|---|
| FLAT(平地) | 0 | なし |
| HIGH(高台) | +1 | 射程が伸びる |
| LOW(窪地) | -1 | 射程が縮む |
| WALL(障害物) | +1 | 射線を遮る/侵入不可 |
| SWAMP(沼地) | -1 | 窪地の効果+そこにいる側の命中率-12%(飛行中は影響なし) |
| RIVER(川) | 0 | FLY以外の移動で、移動元・移動先どちらかが川ならAP消費2 |
| TREE_LOW(低木) | +1 | 障害物相当。隣接するまで判別不能 |
| TREE_HIGH(高木) | +1 | 高台相当だが射線も遮る。隣接するまで判別不能 |

- WALL・TREE_HIGH・未判別の木は「高度2(飛行中)未満のもの同士」の**同レーン上**の射線を遮る
- これらは地上からは進入不可(飛行中のみ通過・上空移動が可能)。WALLの直下には着地できない
- **rangeが`広`の攻撃は、対象が別レーンの場合はこの射線判定の対象外になる**(同レーンを撃つ場合のみ従来通りブロックされる)
- **木々の判別状態はプレイヤー・敵それぞれ別管理**。未判別の木は障害物として扱われ、隣接すると判明する(ただしそのターンは移動が成立しない)

---

## 4. 装備スロット(CLOSET)

| スロットID | ラベル | 装備可能カテゴリー |
|---|---|---|
| R_HAND / L_HAND | RIGHT HAND / LEFT HAND | MELEE, SHOOT, SHIELD |
| **R_BACK** | R-BACK (背部右) | BACK_MELEE, BACK_SHOOT |
| **C_BACK** | C-BACK (背部中央) | BACK_SUPPORT |
| **L_BACK** | L-BACK (背部左) | BACK_MELEE, BACK_SHOOT |
| TOPS / BOTTOMS / INNER / SHOES | 各部位 | 対応カテゴリーのみ |
| EX_CORE | EX CORE (拡張脳) | EX_CHIP |

- 背部は以前「BACK」1スロットで武装・補助が排他だったが、**右・左の2スロットで背部武器を同時装備でき、中央は補助専用**という形に分割済み。理論上、両手+両背部で**最大4つの武器を同時所持**できる
- O.P.A-GENERATOR側のカテゴリー種類(MELEE/SHOOT/SHIELD/BACK_MELEE/BACK_SHOOT/BACK_SUPPORT/TOPS/BOTTOMS/INNER/SHOES/EX_CHIP/EX_MODULE)自体は変更していない。変わったのはCLOSET側のスロット構成のみ

---

## 5. 行動コマンド一覧

| コマンド | AP消費 | 内容・条件 |
|---|---|---|
| `MOVE_F`/`MOVE_B`(前進/後退) | 1 / 失敗時0 | 横位置を1移動 |
| `MOVE_U`/`MOVE_D`(奥へ/手前へ) | 1 / 失敗時0 | レーンを1移動 |
| `MOVE_FU`/`MOVE_FD`(斜め移動) | 1 / 失敗時0 | **横+縦を1手で移動**。レーン数が増えたフィールドで移動が冗長にならないよう追加。敵も同様に斜め移動する。※APの最大値を見直す際にAP2へ変更するか再検討の余地あり |
| `DEFEND`(防御) | 1(固定) | 詳細は6節。**シールド装備時はボタン表記が「シールド防御」に変わり、軽減量も強化される** |
| `FLY`(飛行) | 1(固定) | `height = 2`。**発動時に`flightTurnsLeft`を`maxFlightTurns`にセット**(7節) |
| `LAND`(着陸) | 1 / WALL直下なら0 | 着地。`flightTurnsLeft`をnullに戻す |
| `JUMP` | 1 / 不可なら0 | 敵と同レーン・横位置差1以内でのみ使用可。成功で背面(`isBehind`)を取る |
| `ATK`(攻撃) | 2 / 射程外なら1 / 使用不可なら0 | 6節 |
| `RELOAD`(装填/冷却) | 1 / 不要なら0 | 弾補充 or HEATリセット |
| `SKILL`(アクティブスキル) | **0** | 9節 |
| `PASS`(ターン終了) | 残APを放棄 | **スタンで何もできないターンを進めるために必須**。スタン中や残AP0のときにボタンが出現する。通常時も手番を相手に渡す用途に使える |

---

## 6. 攻撃(ATK)の詳細

### 6-1. 命中判定(`checkAttackValidity`)は2段階になった

**第1段階(既存・射程とレーン)**:

| range値 | baseRange | レーン制約 |
|---|---|---|
| `近` | 1 | レーン距離1以内 |
| `中` | 3 | レーン距離1以内 |
| `遠` | 4 | レーン距離1以内 |
| **`広`(新規)** | **2** | **無し(全レーンが対象)** |
| `列` | - | 同レーンなら距離無制限、レーン違いは必ず失敗 |

```
実効射程 = max(1, baseRange + (自分の高度 - 相手の高度))
命中条件(広・近・中・遠共通): 横位置の距離 <= 実効射程 かつ (広は常にOK / それ以外はレーン距離 <= 1)
```
- WALLによる射線ブロックは「同レーンへの攻撃」の場合のみ判定される。`広`で別レーンを狙う場合はこの判定自体が発生しない(=障害物を飛び越える形の挙動)

**第2段階(新規・SPD回避)**:
第1段階を通過した(=射程内に入った)攻撃に対してのみ、追加で回避判定が入る。
```
回避率(%) = clamp((防御側SPD - 攻撃側SPD) × 1.5, 0, 35)
```
- プレイヤー→敵、敵→プレイヤーの**両方向**に適用される
- 回避が成立した場合、弾薬消費・HEAT蓄積などの副作用は通常通り発生するが、ダメージは0になる
- EX_SPECIALSの`EVASION`(確定回避)とは独立して共存する。判定順は「SPD回避 → (外れたら)EVASION → (無ければ)SHIELDアクティブ → 通常ダメージ」

### 6-2. ダメージ計算式
```
基礎ダメージ = max(1, 攻撃側ATK - 防御側の被ダメ軽減)
```
- 被ダメ軽減:通常は相手のDEF。相手が`DEFEND`中なら**DEF×2**(プレイヤーがシールド装備で防御中の場合は**DEF×3**、7節参照)。背面攻撃時はDEFを参照しない
- `CRITICAL`発動中ならさらに2倍。背面攻撃ならさらに1.5倍
- 素手ダメージは`プレイヤーATK × 0.5`ベース
- プレイヤーのATKには`atkDebuff`、DEFには`defDebuff`が掛かる(敵のWEAKEN/ARMOR_BREAKを受けている場合)

**プレイヤーが受けるダメージの上限**(理不尽な即死を防ぐための安全弁):

| 攻撃 | 上限 |
|---|---|
| 敵の通常攻撃 | 最大HPの**70%** |
| 一斉発射(ALL_FIRE) | 最大HPの**75%** |
| 渾身の一撃(FULL_STRIKE) | 最大HPの**60%** |

> **背景**:敵のATKは「プレイヤーのATK」を基準に算出されるため、攻撃特化の装備ほど敵の火力も跳ね上がる。
> 上限が無いと、防御を固めても通常攻撃1発で沈む状況が実際に発生していた(オーパ戦で確認)。
> この上限により、どんな装備構成でも最低2発は耐えられることを保証している。

---

### 6-3. 敵の武装システム(大規模改修 段階2)

敵も武器を持つ。プレイヤーの装備とは**別系統の専用テーブル**(`ENEMY_WEAPONS`)で管理する。

- 各武器は `atkMult`(敵ATKに掛ける倍率)・`range`・`maxAmmo` または `heatRate` だけを持つ
- ランクによる強さのスケーリングは敵ATK側で自動的に効くため、**テーブルは「射程と性格」だけを定義すればよい**
- 全30種:近接12 / 射撃12 / 背部射撃6

| 系統 | 例 | 管理 |
|---|---|---|
| 近接 | 軍用ブレード / 重斬刀 / パイルバンカー(1.55倍・HEAT65) / 実体ナイフ(0.7倍・HEAT12) | HEAT |
| 射撃 | 軍用ライフル / レールガン(1.45倍・残弾2) / サブマシンガン(0.6倍・残弾10) / 散弾砲(広) | 残弾 |
| 背部射撃 | 背部キャノン(列) / 衛星誘導砲(1.5倍・残弾2) / 背部ガトリング(0.65倍・残弾12) | 残弾 |

**武装構成**は `ENEMY_LOADOUT` でアーキタイプ×ランク(低G〜E / 中D〜B / 高A〜S)ごとに決まる。
同一の敵が同じ武器を2本持たないよう、抽選時に重複を避ける。
※シールドは武器に含めず、従来通り `tankDefend`(防御時の軽減強化)で表現する。

**敵AIの武器選択**:
1. 使用可能(残弾/HEATに余裕がある)な武器を絞り込む
2. そのうち今の位置関係で当たるものを `checkAttackValidity` で判定し、**火力(atkMult)の高い順**に選ぶ
3. 当たる武器が無ければ射程に入るよう前進する
4. 使える武器が尽きたらリロード/冷却を行う(その手番は攻撃できない)

残弾・HEATは敵にも適用されるが、**ENEMY DATAには表示しない**(射程だけを見せ、残弾やリロードは戦闘ログで分かる、という方針)。

---

## 7. シールドとDEFENDの関係(更新)

- 通常のDEFENDは被ダメ計算時に相手DEFを**×2**として扱う
- **`R_HAND`または`L_HAND`にSHIELDタイプを装備している場合、DEFENDの軽減倍率が`×3`に強化される**(`hasShieldEquipped`参照)
- 装備中のシールド名(`shieldName`)がDEFENDボタンのラベル変化・実行時のログの両方に反映される。ダメージ計算式自体(ATK-DEF×N)は変わらない
- SHIELDタイプはこれまで通り`activeWeapons`には入らないため、専用の攻撃手段は持たない(=素手 or 他スロットの武器で攻撃しつつ、DEFENDを強化する運用になる)

---

## 8. 飛行ターン制限(新規)

- BACK_SUPPORTのクラフト時に`flightTurns`(3〜6のランダム値)が付与される
- `FLY`実行時、`flightTurnsLeft = maxFlightTurns`をセット
- **1ラウンド(プレイヤー行動+敵行動)が終了するごとに`flightTurnsLeft`を1減算**。0になった時点で`height`を現在地の地形高度に戻し、「エネルギー切れで強制着地」のログを出す
- `LAND`で自主的に着地した場合も`flightTurnsLeft`はnullに戻る
- **着地後のクールダウンは無い**。再度`FLY`すればその時点の`maxFlightTurns`で即座にリセットされる(1戦闘中に何度でも飛行→着地を繰り返せる)
- 戦闘中は`p.height === 2 && p.flightTurnsLeft !== null`の間、画面上に残ターン数バッジが表示される

---

## 9. アクティブスキル(EX_SPECIALS)の戦闘内挙動

自己バフ系5種に加え、**敵デバフ系3種を追加**。
この節では既存8種を扱う。攻撃系4種・行動阻害系5種を加えた**計17種**の全体像は9-0節を参照。

| ID | 効果 | 実装上の挙動 |
|---|---|---|
| `HEAL` | HP30%回復 | 即時 |
| `CRITICAL` | 次の1撃のダメージ2倍 | `nextCrit`フラグ、次のATKで消費 |
| `EVASION` | 次に受ける攻撃を無効化 | `evasion`フラグ。SPD回避の次に判定される |
| `AP_BOOST` | AP+2 | 即時 |
| `SHIELD` | 次の被ダメージを半減 | `shieldActive`フラグ |
| **`WEAKEN`(新規)** | **敵ATKを戦闘終了まで-30%** | 発動時に`e.atk`を直接`×0.7`して以後持続 |
| **`ARMOR_BREAK`(新規)** | **敵DEFを戦闘終了まで-50%** | 発動時に`e.def`を直接`×0.5`して以後持続 |
| **`STUN`(新規)** | **敵のそのターンの残り手番をすべて奪う** | `e.stunned`フラグと`e.ap = 0`をセットする。フラグは`endTurn`でまとめて解除され、1行動では消費しない。交互行動制では「1行動だけ封じる」方式がほぼ無意味になったための変更(v1.27.2) |

- APを一切消費しない。1戦闘につき、同じ効果を複数装備していればその個数分だけ使える(既存仕様のまま)
- `WEAKEN`/`ARMOR_BREAK`は「発動したら戦闘終了まで持続」する一発モノ(期間管理の仕組みは追加していない)

### 9-0. 攻撃系・行動阻害系スキル9種(大規模改修 段階4)

EX_SPECIALSは既存8種+新規9種の**計17種**になった。クラフト確率は既存と同じ扱い。
**スキルは全て射程無視**(オーパスキルと同じ扱い)で、位置取りに関係なく当たる。

**攻撃系4種**

| ID | 効果 |
|---|---|
| `PIERCE_SHOT`(貫通射撃) | ATK1.5倍・**防御無視** |
| `BLAST_ROUND`(爆裂弾) | ATK1.2倍+**敵を1マス吹き飛ばす**(接近戦からの脱出手段。壁際では吹き飛ばせない) |
| `BURN_SHOT`(焼夷弾) | ATK0.8倍を**3ターン継続**(合計2.4倍だが即効性は無い) |
| `RECKLESS`(捨て身の一撃) | ATK2.5倍・**自分も最大HPの10%を失う**(最低HP1で踏みとどまり自滅はしない) |

**行動阻害系5種**(数値デバフと違い「何ができなくなるか」で駆け引きを作る)

| ID | 効果 |
|---|---|
| `LOCK_MOVE`(移動封鎖) | 1ターン移動不可(攻撃・防御・スキルは可) |
| `LOCK_ATTACK`(武装凍結) | 1ターン攻撃不可(移動・防御・スキルは可) |
| `BREAK_ARMS`(武装破壊) | 相手の武器をランダムに1つ使用不能に(射撃=残弾0 / 近接=強制オーバーヒート) |
| `SEAL_SKILL`(スキル封印) | 1ターン相手のスキル使用を封じる |
| `GRAVITY`(重力異常) | 1ターン相手の移動APを2倍にする |

- いずれも**敵側も使用してくる**(A/Sランクのスキルプールに追加済み)
- 状態は `lockMoveTurns` / `lockAttackTurns` / `sealSkillTurns` / `gravityTurns` / `burnTurns` として
  プレイヤー・敵の双方に持たせ、`endTurn` でまとめて消化する
- 炎上ダメージで決着がついた場合の勝敗判定もある

> **重力異常の実装差**:プレイヤーは移動コストを2倍にするが、**敵は「1手番=AP1」固定で動く**ため
> 同じ方法が使えない。敵側は「移動時に余分に1手番を消費する」形で表現している(1ターンに動ける
> 回数が実質半分になる)。**飛行中は双方とも重力異常の影響を受けない**(地面に押さえつけられる
> 効果であるため)。

---

### 9-1. オーパ専用スキル4種(実装済み)

Sランクの防衛戦で稀に現れる「オーパ」を撃破した時のみ入手できる特別なスキル。既存8種とは別枠の新規スキルで、破格の性能と大きな後隙を併せ持つ。詳細は「ARENAランク制度 仕様メモ」7-5節を参照。

| ID(仮) | プレイヤー版の効果 | 後隙(デメリット) |
|---|---|---|
| `ALL_FIRE`(一斉発射) | 装備中の全射撃武装の合計ATK分のダメージ | 全射撃武装の残弾が強制0 |
| `FULL_STRIKE`(渾身の一撃) | 位置無関係に背後を取る+ATK×2の防御貫通 | 2ターンスタン |
| `AUTO_REPAIR`(オートリペア) | 3ターン、被ダメージの半分を回復 | 効果中はFLY不可 |
| `AP_HIGH_BOOST`(APハイブースト) | AP+4 | 無し |

オーパ自身もこの4種を所持し使用してくる。敵版は以下のように読み替えている:

| ID | 敵(オーパ)版の効果 | 後隙 |
|---|---|---|
| `ALL_FIRE` | ATK×2.5のダメージ(上限:プレイヤー最大HPの75%) | 2ターン攻撃不可(`noAttackTurns`)。その間は距離を取る/回り込む |
| `FULL_STRIKE` | 背後へ回り込み+ATK×2の防御貫通(上限:同60%) | 2ターン行動不能(`stunTurns`)。**ただし防御態勢に入る**(被ダメ半減) |
| `AUTO_REPAIR` | 3ターン、被ダメージの半分を回復(`repairTurns`) | 効果中は移動不可 |
| `AP_HIGH_BOOST` | 3行動 | 無し |

**使用条件**:後隙の大きい2つは、追い詰められてから撃って自滅しないよう、HPに余裕がある時のみ使う
(`FULL_STRIKE`はHP50%以上、`ALL_FIRE`はHP40%以上)。
`FULL_STRIKE`の後隙中に防御態勢へ入るのは、「スキルを撃たせれば一方的に倒せる」という必勝パターンを防ぐための措置。

---

## 10. 敵ターンの行動ロジック(更新)

**プレイヤーが1行動するたびに、1手番ぶん実行される**(交互行動制。`runEnemyAction()`)。判定順:

1. **`e.stunned`がtrueなら何もせず行動終了**(スキルも使えない)。フラグは`endTurn`まで解除されないため、そのターンの残り手番もすべて失われる
2. **ライバル「ティニー」のリミッター解除**(未使用かつ2ターン目以降)
3. **アクティブスキル判定**(所持していて未使用のもののみ。1ターン1つ):
   WEAKEN/ARMOR_BREAK(初手)→ STUN(2ターン目以降)→ SHIELD(HP50%未満)→ HEAL(40%未満)→ EVASION(30%未満)
4. スキルを使わなかった場合、**アーキタイプごとの行動指針**に沿って判定(10-1節)
5. **行動後、AP_BOOSTを所持していて、移動したがまだ接近が必要なら発動**(もう1歩踏み込む=2行動)

移動時は木々・川の影響も受ける(未判別の木に入ろうとすると判別のみで移動失敗、川がらみの移動は50%で失敗)。
移動処理は`tryEnemyStep()`に共通化されており、敵も**斜め移動**が可能。

### 10-1. アーキタイプごとの行動指針

以前は全アーキタイプが同じ判定順・同じ確率で動いていたため、「ステータスが違うだけ」で戦い方に個性が無かった。
現在は`archetypeProfile`で型ごとにパラメータを変えている:

| 型 | preferredDist | 防御閾値/確率 | laneAlignChance | 狙い |
|---|---|---|---|---|
| アタッカー | 1 | HP20%未満 / 30% | 85% | 常に接近して殴る。防御はほぼしない |
| タンク | 1 | HP50%未満 / 50% | 85% | 前に出て粘る。早めに防御を固める |
| 回避型 | 3 | HP30%未満 / 30% | 50% | 距離3を維持。近づかれたら離れる/回り込む |
| 照準型 | 2 | HP30%未満 / 30% | 100%(laneFirst) | 射線の確保を最優先。レーン合わせを最初に判定 |
| **空戦型** | 3 | HP30%未満 / 30% | 70% | 射程圏に入ると上空へ。高度差ぶん射程を伸ばして撃ち下ろす |
| **強襲型** | 1 | HP20%未満 / 20% | 90% | 距離があれば飛び立ち、障害物も木も無視して最短距離で詰めて殴る |

**アーキタイプのステータス補正と解禁ランク**:

| 型 | 補正 | 解禁 | 武装傾向 |
|---|---|---|---|
| アタッカー | ATK+30% | G〜 | 近接中心 |
| タンク | DEF+40% HP+30% / 防御時軽減強化 | G〜 | 近接+シールド相当 |
| 回避型 | SPD+30% EVA+15 | D〜 | 射撃中心 |
| 照準型 | ACC+15 | B〜 | 射撃+背部射撃 |
| **空戦型** | SPD+15% ACC+10 / `canFly` | D〜 | 射撃中心 |
| **強襲型** | ATK+35% SPD+20% / `canFly` | A〜 | 近接中心 |

判定順(スキルを使わなかった場合):

0. **飛行判断**(`canFly` の型のみ):地上にいて飛行残ターンが未消費なら、
   強襲型は「距離が離れていれば」、空戦型は「射程圏に入ったら」70%で離陸する。
   **離陸そのものが1手番を消費する**
1. **照準型のみ**:レーンがズレていれば最優先で合わせる(`laneFirst`)
2. **離れたい状況**(`wantsRetreat` = 一斉発射の後隙中、または回避型が`preferredDist`より近づかれた):
   後退 → 塞がれていたら側面へ回り込む → それも無理なら防御
3. **回避型のみ**:間合いが保てていても50%でレーンをずらす(`wantsJuke`。的を絞らせない立ち回り)
4. レーンがズレていれば`laneAlignChance`で合わせる。**横距離も離れていれば斜めに詰める**
5. 横距離が`preferredDist`より大きければ前進
6. HPが`defendThreshold`未満なら`defendChance`で防御
7. 一斉発射の後隙中(下がれなかった場合)は防御しつつ再装填
8. いずれにも該当しなければ攻撃(**所持武器から選択**。6-3節)。**攻撃直前にCRITICALを所持していれば発動**
   - 射線が通らない場合は棒立ちせず、レーンを変えて回り込みを図る
     (これが無いと壁越しに両者が動かない膠着状態が延々と続く)

### 10-2. 敵の飛行(大規模改修 段階3)

`canFly` を持つ型(空戦型・強襲型)は飛行できる。**プレイヤーと同じルール**:

- 飛行中は障害物・木を無視して移動でき、川の影響も受けない
- 高度2を維持するため、移動しても地形の高さに引きずられない
- **飛行には残ターン制限(4ターン)があり、尽きると強制着地する**
  - 常時飛ばれるとプレイヤーの近接装備が完全に無力化されるため、この制限は必須
- 離陸そのものが1手番を消費する
- 重力異常の影響を受けない

> **強襲型の脅威**:空から最短距離で詰めてくるため、**遮蔽物に隠れる戦法が通用しない**。
> A以降限定にすることで上位ランクの脅威として立たせている。

被ダメージ処理(敵の攻撃がプレイヤーに命中判定を通過した場合):
```
① SPD回避判定(敵のACC・沼地ペナルティを加味) → 回避なら0ダメージ
② (回避されなければ)EVASIONスキル中か？ → 100%無効化
③ (それも無ければ)SHIELDアクティブ中か？ → floor(x/2)
④ 通常ダメージ = max(1, 敵ATK - (プレイヤー防御中なら 実効DEF×(シールド装備なら3、通常なら2)、そうでなければ実効DEF))
   ※実効DEF = プレイヤーDEF × defDebuff(敵のARMOR_BREAKを受けていれば0.5)
   ※敵がCRITICALを発動していればダメージ2倍
```

プレイヤーの攻撃時も、敵がEVASION/SHIELDを構えていれば無効化/半減される。プレイヤーのATKは`atkDebuff`(敵のWEAKENを受けていれば0.7)が掛かる。

ターン終了時、プレイヤー・敵ともに`ap = MAX_AP`(=5)にリセットされ、`turn`が1加算される。**このタイミングで飛行残ターンの減算・強制着地判定、およびSTUNによるプレイヤーAP0化も行われる**。

---

## 11. 勝敗・終了処理(変更なし)

- 敵HP0以下 → `WIN`。マテリアル(敵の`reward`ぶん)を獲得
- プレイヤーHP0以下(敵ターン処理後) → `LOSE`
- 結果画面から`battleState`をnullに戻しARENA入口画面へ

---

## 11-2. 戦闘画面のUI構成(新規)

情報量が増えたため、「自分の情報」と「敵の情報」を画面の上下で分離している。

### ヘッダー(上部)= 自分の状態と位置関係

| 位置 | 内容 |
|---|---|
| 左 | HP / AP / **ATK・DEF・SPD**(デバフ中は「345→241」と元の値と実効値を併記)/ 自分の発動中スキル(完全回避・防壁・急所狙い・自己修復・反動・STUN) |
| 中央 | DIST(横距離)/ **TURN数** / フィールド名 |
| 右 | **自分と敵の座標**(例 1-4 / 6-1)/ 各々の現在地形 / **高低差とその効果**(例「高低差 +2(射程+2)」) |

> 高低差を効果込みで明示しているのは、射程補正に直結するにも関わらず読み取りにくかったため。
> 座標表示はフィールドサイズ可変で位置関係が掴みにくくなったことへの対応。

### 下部パネル = 戦闘中は ENEMY DATA、戦闘外はシステムログ

戦闘中の全体ログはほぼ流れない(大会の回戦通過程度)ため、そのスペースを敵情報に充てている。
`App`が`activeEnemy`ステートを持ち、`ArenaBattle`から`setActiveEnemy`で同期する。戦闘終了時はnullに戻り通常のログ表示に戻る。

表示内容:
- 敵の名前(ライバルは★、オーパは専用色)
- HP/ATK/DEF/SPD + **HPバー**(残量に応じて赤→橙→黄)
- **アーキタイプ名とその戦い方の説明**(例「回避型:距離を取って戦う。近づくと離れる/レーンをずらす」)
- **所持スキル一覧**(使用済みは「(済)」でグレーアウト)。何が飛んでくるか事前に読める
- 現在の状態(リミッター解除/回避構え/防壁/狙撃準備/自己修復/反動で行動不能/再装填中。残ターン付き)

> **実装済み**(v1.28.0):ENEMY DATAの「ARMS」欄に所持武装の名前と射程(近/中/遠/列/広)を
> 一覧表示している。残弾とHEATは表示しない方針(戦闘ログで分かるため)。

### レイアウト上の注意点

- 下部の操作パネルは**固定高さ(h-56)**。移動ボタン群・武器ボタン群は**最低高さ指定+`shrink-0`**にしてあり、
  比率指定にすると画面が狭い時にボタンが潰れて画面外へ押し出される(実際に発生した)。
- コマンド欄は`overflow-y-auto`。収まらない場合はスクロールで到達できる。
- フィールドの高さ・ファイターの表示サイズはレーン数連動(`140 + lanes*30`px / 3レーン=120px・4=105px・5以上=90px)。

---

## 12. 現状の制約・今後の拡張検討に関わる注意点(更新)

- ~~SHIELDタイプの装備は攻撃手段を持たない~~ → **DEFENDの強化という形で存在意義は持たせた**。ただし専用の攻撃手段が無い点は変わらず
- ~~背部ユニットは1つしか持てない~~ → **解消済み**。両手+両背部で理論上最大4武器の同時運用が可能になった。**行動の密度・バランス感がこれまでより上がっている点は引き続き注視が必要**
- 武器の`range`値は`近`/`中`/`遠`/`列`/`広`の**5種**に拡張。新カテゴリー傾向を追加する場合もこの範囲内での調整になる
- ~~敵AIの意思決定パターンは全アーキタイプで共通~~ → **解消済み**(10-1節)。型ごとに間合い・防御頻度・射線の意識が変わるようになった
- ~~敵は「1ターン1行動」「武器を持たない」「攻撃判定は常に`range: '近'`固定」~~ → **大規模改修で全て解消**
  - 敵もAP制(MAX_AP5)で、プレイヤーと1行動ずつ交互に戦う(1節)
  - 敵も武器を持ち、射程・残弾・HEATを管理する(6-3節)
  - 敵も飛行できる(10-2節)
- **残っている非対称さ**(現時点では意図的にそのままにしている):
  - 敵は「1手番=AP1」固定で動く。プレイヤーのように行動ごとにAP消費が変わらないため、
    重力異常などは「余分に1手番を消費させる」形で読み替えている
  - 敵は`JUMP`(背面取り)・`DEFEND`のシールド強化・EXチップ/モジュールの仕組みを持たない
  - `AP_BOOST`は敵版では「2行動」、`ALL_FIRE`は「ATK×2.5+2ターン攻撃不可」という読み替えのまま
- EX_SPECIALSは**計17種**(自己バフ5/敵デバフ3/攻撃系4/行動阻害系5)。
  行動阻害系と焼夷弾の導入で、**Nターンで解除される時限式の状態**が一般的な仕組みとして入った
  (以前はティニーのリミッター解除だけが例外的に時限式だった)
- **新スロットを追加する場合の注意点は今回R_BACK/C_BACK/L_BACKで解消済み**だが、`activeWeapons`構築や武器ボタンUIは依然として配列の決め打き(`['R_HAND','L_HAND','R_BACK','L_BACK']`)になっているため、将来さらにスロットを増やす場合は同様の複数箇所の修正が必要になる

---

## 13. 関連データ構造(更新)

`battleState.player`の形:
```js
{
  hp, maxHp, pos, lane, height, ap, defActive, type, isBehind, hasFly,
  maxFlightTurns, flightTurnsLeft,
  hasShieldEquipped, shieldName,
  atkDebuff, defDebuff,                   // 敵WEAKEN/ARMOR_BREAKによる倍率(1=無し)
  stunnedNextTurn, isStunnedNow,          // STUN予約フラグ / 表示用の現在スタン中フラグ
  selfStunTurns,                          // 渾身の一撃の反動による自己スタン残ターン
  repairTurns,                            // オートリペア残ターン(効果中はFLY不可)
  // 段階4:行動阻害・継続ダメージ(いずれもendTurnで消化)
  lockMoveTurns, lockAttackTurns,         // 移動不可 / 攻撃不可
  sealSkillTurns, gravityTurns,           // スキル使用不可 / 移動APが2倍
  burnTurns, burnDamage,                  // 焼夷弾のスリップダメージ
  nextCrit, evasion, shieldActive
}
```
`battleState.enemy`の形:
```js
{
  name, isRival, rivalKey, hp, maxHp, atk, def, spd, eva, acc,
  tankDefend, archetype, pos, lane, height, ap, defActive, type, power, reward,
  hasLimiterRelease, limiterActive, limiterTurnsLeft, limiterBonus, limiterUsed,  // ティニー専用
  isOopa, baseName,                       // オーパか / アーキタイプ表記を除いた素の名前(大会の重複出現防止)
  skills, usedSkills,                     // 所持スキル / 使用済みスキル
  nextCrit, evasion, shieldActive,        // 敵自身のスキル発動状態
  noAttackTurns,                          // 一斉発射の後隙(攻撃不可)
  stunTurns,                              // 渾身の一撃の反動スタン(この間は防御態勢)
  repairTurns,                            // オートリペア残ターン(効果中は移動不可)
  stunned,                                // スタン(ターン単位。endTurnで解除)
  // 段階2:武装と飛行
  weapons,                                // [{name, wType, range, atkMult, currentAmmo/maxAmmo, currentHeat/maxHeat/heatRate}]
  maxFlightTurns, flightTurnsLeft,        // 飛行の残ターン(空戦型・強襲型が使用)
  // 段階4:プレイヤーの行動阻害スキルを受けた状態
  lockMoveTurns, lockAttackTurns, sealSkillTurns, gravityTurns,
  burnTurns, burnDamage
}
```
`battleState`のその他:
```js
{
  turn, field: { name, variant },         // 選出された戦闘フィールド
  revealedTreesP, revealedTreesE,         // 木々の判別状態(配列。Set不可。6-3節の注意参照)
  logs, activeWeapons, usedSkills, result
}
```
`activeWeapons`のキー:`R_HAND` / `L_HAND` / `R_BACK` / `L_BACK`(以前は`BACK`1つだった)

`App`が持つ戦闘関連ステート:
```js
activeEnemy   // 戦闘中の敵情報。下部パネルをENEMY DATA表示に切り替えるために使う(戦闘外はnull)
```

---

## 14. ビジュアルアセットの実装状況(新規)

### 立ち絵(`CHARA_ART`)

| キー | 内容 | 状態 |
|---|---|---|
| `A` | TYPE-A | 正規絵(実装済み) |
| `B` | TYPE-B | 正規絵(実装済み) |
| `C` | TYPE-C | 正規絵(実装済み) |
| `OOPA` | オーパ | **暫定**:差し替え前のTYPE-A立ち絵を流用中。専用絵ができたら差し替える |

描画は `InteractiveFighterSVG` が `CHARA_ART[typeId]` を引く。
**未登録のtypeIdはSVGプレースホルダーにフォールバック**するため、
ライバル4体などの絵を追加する場合も `CHARA_ART` にキーを足すだけでよい。
敵の描画時は `e.isOopa ? 'OOPA' : e.type` で出し分けている。

### 武器画像(`WEAPON_VISUALS`)

- `sword_sample`(サンプルソード)のみ実装済み。正規絵に差し替え済み
- 手のアンカーとグリップ座標を持ち、装備時にキャラの手に重ねて描画される

### 画像の最適化方針

立ち絵はHTMLにbase64で埋め込むため、そのままだとファイルが肥大化する。
**透明余白の切り詰め(`getbbox`でcrop)+ 減色(200色・FASTOCTREE)** を行うことで、
立ち絵4枚+武器1枚を追加しても全体を630KBに収めている(最適化前の旧TYPE-A絵だけで237KBあった)。

### 未実装

- ライバル4体の専用立ち絵(現状はSVGプレースホルダー)
- 大会限定アイテム・オーパ報酬の専用ビジュアル(通常アイテムと同じ色スウォッチ表示)
- ユニークエフェクトの視覚的な描画(データ付与のみ実装済み)
- 型(シルエット)画像を使ったペーパードール本実装
